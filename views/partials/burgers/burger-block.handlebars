<li>
   <h3>{{ burgerName }}</h3>
   <p>{{ burgerToppings }}</p>
   {{#if eat}}
   <button class="eat-burger" data-id="{{ id }}" data-burger="{{eat}}">Eat Now!</button>
   {{/if}}
   <button class="delete-cat"></button>

</li>